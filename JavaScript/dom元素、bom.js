// DOM 文档对象模型
// dom元素添加
// appendChild
// insertBefore
// insertAdjacentHtml  第一个参数为 beforebegin afterbegin beforeend afterend

// 元素删除
// remove  parent.removeChild(target)
// $('#taeget').remove()

// 移动dom dom元素位置改变
// h1.insertAdjacentElement('afterend',h2)

// dom替换
// parnetNode.replaceChild(newChild, oldChild)
// oldChild.replaceWith(newChild)

// dom元素查找
// matches  target.matches('.btn')  匹配选择器
// contains parentNodes.contains(child)

// BOM浏览器对象模型
// wondow.innerHeight window.innerWidth  window.open window.close() window.resizeTo()
